// Correspond à la fonction Trouve disponible
FONCTION Tri-insertion (Tableau T)
    VARIABLE
        ENTIER: x <- T[i]
         j ← i    
    DEBUT
        TANTQUE j > 0 et  T[j-1] > x
                T[j] <- T[j-1]
                j <- j-1 
                T[j] <- x
        Retourne i
    FIN
FINFONCTION